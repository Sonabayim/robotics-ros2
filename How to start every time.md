When you are running matlab you have to take into account ROS domain id, open new terminal and source the environment `source ~/.bashrc` and run this command 

```bash 
export ROS_DOMAIN_ID=30
matlab &
```


